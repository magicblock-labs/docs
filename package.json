{
  "name": "magicblock-docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "mintlify dev",
    "build": "mintlify validate",
    "start": "mintlify dev",
    "export": "mintlify export",
    "validate": "mintlify validate",
    "update": "mintlify update",
    "localize:prepare": "node scripts/localize-with-openai.mjs --prepare-only",
    "localize:translate": "node scripts/localize-with-openai.mjs --translate --concurrency=2",
    "localize:check": "node scripts/check-localization.mjs"
  },
  "dependencies": {
    "recharts": "^3.3.0"
  },
  "devDependencies": {
    "@mintlify/cli": "^4.0.1124"
  }
}
