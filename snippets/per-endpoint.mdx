<Note>
  Private Ephemeral Rollup (devnet) endpoint: `https://tee.magicblock.app?token=
  {authToken}`. Replace `{authToken}` with your authorization token obtained
  from the TEE RPC to send requests.
</Note>
