<Note>
  Private Ephemeral Rollup (devnet) endpoint: `https://tee.magicblock.app?token=
  {authToken}` Use these endpoints to send requests. Replace `{authToken}` with
  your authorization token obtained from the TEE RPC.
</Note>
