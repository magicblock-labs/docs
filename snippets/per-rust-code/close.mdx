```rust
use ephemeral_rollups_sdk::access_control::{
    instructions::ClosePermissionCpiBuilder
};

ClosePermissionCpiBuilder::new(&permission_program)
    .payer(&payer)
    .authority(&payer, false)
    .permissioned_account(&permissioned_account, true) // program is permissioned_account owner
    .permission(&permission);
    .invoke_signed(&[seed_refs.as_slice()])?; // seeds of permissioned_account + bump
```
